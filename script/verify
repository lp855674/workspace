#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR"

echo "=== verify: 1/2 check-db-boundary ==="
bash "$ROOT_DIR/script/check-db-boundary"

echo "=== verify: 2/2 cargo check ==="
cargo check

echo "=== verify passed ==="
